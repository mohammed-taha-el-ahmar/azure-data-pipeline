{
  "name": "CopyApiToADLS",
  "properties": {
    "description": "Copies the latest payload from the source REST API into the 'raw' container in ADLS Gen2. Requires a REST linked service (source) and an ADLS Gen2 linked service (sink), plus the OpenMeteoApiDataset / ADLSRawDataset datasets referenced below, to be configured in the Data Factory authoring UI before this pipeline can be imported and run.",
    "activities": [
      {
        "name": "CopyToRaw",
        "type": "Copy",
        "typeProperties": {
          "source": {
            "type": "RestSource",
            "httpRequestTimeout": "00:01:40",
            "requestInterval": "00.00:00:00.010"
          },
          "sink": {
            "type": "JsonSink",
            "storeSettings": {
              "type": "AzureBlobFSWriteSettings"
            }
          }
        },
        "inputs": [
          {
            "referenceName": "OpenMeteoApiDataset",
            "type": "DatasetReference"
          }
        ],
        "outputs": [
          {
            "referenceName": "ADLSRawDataset",
            "type": "DatasetReference"
          }
        ]
      }
    ]
  }
}
